import { createSlice } from "@reduxjs/toolkit";
const initialState = {
  data: [],
};
const ProductData = createSlice({
  name: "produc-data",
  initialState,
  reducers: {},
});
export const {} = ProductData.actions;

export default ProductData.reducer;
